{
  "name": "viberant",
  "version": "0.1.0",
  "private": true,
  "description": "Open one project across your AI apps.",
  "main": "desktop/main.js",
  "scripts": {
    "start": "node app/server.mjs",
    "desktop": "electron .",
    "workspace:service": "node app/plane-service.mjs",
    "workspace:relay": "node app/relay.mjs",
    "workspace:cloudflare:dev": "npx wrangler dev --config deploy/cloudflare-workspace/wrangler.jsonc",
    "workspace:cloudflare:deploy": "npx wrangler deploy --config deploy/cloudflare-workspace/wrangler.jsonc",
    "icon": "node build/icon.mjs",
    "build": "node build/require-oauth.mjs && node build/icon.mjs && electron-builder --win --x64",
    "test": "node --test \"core/reference/test/*.test.mjs\" && node --test \"app/test/*.test.mjs\""
  },
  "build": {
    "appId": "com.viberant.manager",
    "productName": "Viberant",
    "artifactName": "Viberant-Setup-${version}.${ext}",
    "asar": false,
    "files": [
      "desktop/**",
      "app/**",
      "runtime/git/**",
      "build/icon.png",
      "core/reference/src/**",
      "package.json",
      "!**/test/**",
      "!**/*.md"
    ],
    "directories": {
      "output": "dist"
    },
    "electronLanguages": [
      "en-US"
    ],
    "win": {
      "icon": "build/icon.ico",
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "Viberant"
    }
  },
  "devDependencies": {
    "electron": "^43.3.0",
    "electron-builder": "^26.15.3"
  }
}
